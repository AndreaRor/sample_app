<li><strong>" <%= micropost.content %> " </strong> 
     <% if micropost.user_id == current_user.id %>
     	<%= link_to "Delete", micropost, :method => :delete, 
     									 :confirm => "Supprimer votre post?", 
     									 :title => "Supprimer micropost" %>
     <% end %>
	 <br/> 
     Posté il y a <%= time_ago_in_words(micropost.created_at) %> par <%= User.find_by_id(micropost.user_id).nom %>
</li>